# Authoring / preview artifacts (not part of the deployed site)
.image-slots.state.json
.thumbnail

# Build output — generated by build.py / build_localbuzz.py, never commit
dist/
localbuzz/dist/

# Onboarding import drafts — generated by import_tool/, per-restaurant scratch space
onboarding/

# Per-restaurant working sites (config + preview build) — staging area before
# each restaurant graduates to its own deploy repo; build with `build.py --site <slug>`
sites/

# Python cache (import_tool/)
__pycache__/
*.pyc

# OS / editor junk
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
