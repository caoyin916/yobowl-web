# Authoring / preview artifacts (not part of the deployed site)
.image-slots.state.json
.thumbnail

# Build output — generated by build.py, never commit
dist/

# OS / editor junk
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
